apiVersion: v1
kind: Service
metadata:
  name: app-np-srv
spec:
  type: NodePort
  selector:
    app: loadtestingapp
  ports:
    - name: loadtestingapp
      protocol: TCP
      port: 80
      targetPort: 80